{
  "name": "pysets",
  "version": "0.0.2",
  "scripts": {
    "release": "git tag -a v$(cat package.json | jq -r \".version\") -m \"v$(cat package.json | jq -r \".version\")\" && twine upload dist/*"
  }
}
